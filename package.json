{
  "name": "@robosystems/core",
  "version": "0.8.8",
  "description": "Shared RoboSystems frontend core library (auth, contexts, task monitoring, UI components) for the RoboSystems Next.js apps",
  "license": "MIT",
  "private": false,
  "type": "module",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RoboFinSystems/robosystems-core.git"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "feature:create": "./bin/create-feature.sh",
    "release:create": "./bin/create-release.sh",
    "clean": "rm -rf dist *.tgz",
    "build": "npm run clean && tsc -p tsconfig.build.json && node scripts/prepare-package.mjs",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "pack:local": "npm run build && npm pack ./dist",
    "test": "vitest run --silent",
    "test:watch": "vitest watch --silent",
    "test:coverage": "vitest run --coverage --silent",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "test:all": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "peerDependencies": {
    "@robosystems/client": ">=1.10 <2",
    "flowbite-react": "^0.12.5",
    "next": ">=15 <17",
    "react": ">=18 <20",
    "react-dom": ">=18 <20",
    "react-icons": ">=4 <6"
  },
  "dependencies": {
    "react-markdown": "^10",
    "remark-gfm": "^4.0.1",
    "tailwind-merge": "^3"
  },
  "//overrides": "deepmerge-ts is forced to ^8 to clear GHSA-ggr8-5vv4-36mx (stack exhaustion on recursive object graphs). It arrives through the flowbite-react peer, which pins 7.1.5 exactly; npm's only other remedy is downgrading flowbite-react. REMOVE once flowbite-react ships a release depending on deepmerge-ts 8. Note that npm ignores overrides when this package is consumed as a dependency, so this protects our own dev and CI tree only - consumers carry their own.",
  "overrides": {
    "deepmerge-ts": "^8.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@next/eslint-plugin-next": "^16.2.10",
    "@testing-library/jest-dom": "^7",
    "@testing-library/react": "^16",
    "@testing-library/user-event": "^14",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^6.1.1",
    "@vitest/coverage-v8": "^4.1.9",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-prettier": "^5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "jsdom": "^29.1.1",
    "prettier": "^3",
    "prettier-plugin-organize-imports": "^4",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "prop-types": "^15.8.1",
    "tailwindcss": "^4.3.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8",
    "vite": "^8.2.2",
    "vitest": "^4.1.9"
  }
}
